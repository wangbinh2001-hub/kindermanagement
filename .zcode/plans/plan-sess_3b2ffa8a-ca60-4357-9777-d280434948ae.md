Fix Phase 12 blockers:
1. Add ParentRequest model + enums to Prisma schema
2. Add Menu model (Phase 9 nutrition) to Prisma schema  
3. Fix Invoice enum - parent-portal.ts references SENT/PARTIAL/OVERDUE but schema uses ISSUED/PARTIALLY_PAID
4. Fix students test schema (limit -> pageSize)
5. Run typecheck to verify all errors cleared
Then run full CI pipeline